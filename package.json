{
  "name": "bpr-intelligence",
  "version": "1.0.0",
  "description": "AI-powered intelligence platform for premium Brazilian real estate",
  "private": true,
  "scripts": {
    "generate:book": "node bpr-project/src/create-book.js",
    "generate:deck": "node bpr-project/src/create-pitch-deck.js",
    "generate": "npm run generate:book && npm run generate:deck",
    "test": "jest --verbose",
    "test:ci": "jest --ci --coverage --forceExit",
    "lint": "eslint bpr-project/src/ --ext .js",
    "lint:fix": "eslint bpr-project/src/ --ext .js --fix"
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "jest": "^30.2.0"
  },
  "dependencies": {
    "docx": "^9.5.3",
    "pptxgenjs": "^4.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
